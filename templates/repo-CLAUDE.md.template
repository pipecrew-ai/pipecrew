# {{REPO_NAME}}

{{REPO_PURPOSE}}

## Agent guidelines

- Before planning any change, read `{{AGENT_CONTEXT_DIR}}/AGENT_INDEX.md` — it maps tasks to the relevant feature, service, and convention files.
- When you add, change, or restructure a feature, integration, or module, update the matching file under `{{AGENT_CONTEXT_DIR}}/` in the same change. Docs must not drift behind the code.
- Write or update tests for every change; all tests must pass before a change is considered done.
{{REPO_SPECIFIC_ABSOLUTES}}

## Quick facts

<!-- One concept per bullet. Group by dimension (Language/build · UI · Data/forms · i18n · Testing · Tooling · …). Don't emit a single paragraph-style bullet. Strip this comment before writing. -->
{{QUICK_FACTS}}

## Build & run

```{{BUILD_LANG}}
{{BUILD_COMMANDS}}
```

## Must-know guidelines (repo-specific)

<!-- Max 10 bullets (validator enforces). Each bullet ≤ one line. One concept per bullet — split "fact + rule" into two bullets if they mix. Strip this comment before writing. -->
{{MUST_KNOW_GUIDELINES}}

## Deep context (topic → file)

| Topic | File |
|---|---|
| Index + feature catalogue + directory tree | `{{AGENT_CONTEXT_DIR}}/AGENT_INDEX.md` |
{{DEEP_CONTEXT_ROWS}}

For feature-specific deep-dives and churning topics (per-feature docs, per-service endpoint inventories), start from `AGENT_INDEX.md` — it points to the right file per task.

## Agent workflow pointers

{{WORKFLOW_POINTERS}}
