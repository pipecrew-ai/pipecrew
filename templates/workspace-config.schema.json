{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Feature Pipeline Workspace Config",
  "description": "Generated by /init. Lives at ~/.claude/{workspace-slug}-config.json. Never committed to any repo (contains absolute paths).",
  "type": "object",
  "required": ["workspace", "repos", "services"],
  "properties": {
    "workspace": {
      "type": "object",
      "required": ["name", "slug"],
      "properties": {
        "name": {
          "type": "string",
          "description": "Human-readable workspace name"
        },
        "slug": {
          "type": "string",
          "pattern": "^[a-z0-9][a-z0-9-]*[a-z0-9]$",
          "description": "Kebab-case slug used in file paths and pipeline dirs"
        },
        "pipeline_dir": {
          "type": "string",
          "deprecated": true,
          "description": "DEPRECATED — no longer read by any skill or script. Superseded by {workspace_root}/{slug}/runs/deliver/ (see /deliver pre-flight). Still accepted on existing configs for back-compat, but /discover no longer emits it; safe to delete."
        },
        "primary_language": {
          "type": "string",
          "default": "en",
          "description": "Primary UI language (en, ar, etc.)"
        },
        "memory": {
          "type": "object",
          "description": "Opt-in GitHub-backed workspace memory. Absent ⇒ feature off (workspace dir stays local-only, current default). When enabled, the durable docs are committed (redacted) + pushed to a PRIVATE repo at the learning checkpoints. See docs/design/github-memory.md.",
          "properties": {
            "enabled": { "type": "boolean", "default": false, "description": "Master switch. When true, /discover bootstraps the repo and the learning skills sync via scripts/sync-memory.js." },
            "remote": { "type": "string", "description": "Git remote URL of the private memory repo (set at bootstrap, e.g. git@github.com:org/{slug}-memory.git)." },
            "repo_name": { "type": "string", "description": "Repo name to create at bootstrap if no remote is given (default: {slug}-memory)." },
            "visibility": { "type": "string", "enum": ["private"], "default": "private", "description": "MUST be private — the memory may reference secrets even after redaction. Bootstrap refuses any other value." },
            "sync_mode": { "type": "string", "enum": ["commit", "pr", "hybrid"], "default": "commit", "description": "How sync-memory.js publishes. 'commit' (default, solo): pull --rebase then commit straight to main. 'pr': always push a memory/<checkpoint> branch + open a PR for review. 'hybrid' (recommended for team-shared repos): structural changes (context/platform.md or context/adrs/**) open a PR; bookkeeping (audit-findings, observability, learn-log, config.portable) commits directly. A rebase conflict in any mode falls back to PR. See docs/design/github-memory.md." }
          },
          "required": ["enabled"]
        }
      }
    },
    "repos": {
      "type": "object",
      "description": "Map of repo short-name → repo config. Each key is a unique identifier used internally.",
      "additionalProperties": {
        "type": "object",
        "required": ["path", "type", "role"],
        "properties": {
          "path": {
            "type": "string",
            "description": "Absolute path to the repo root on disk (local, machine-specific — gitignored from the memory repo)"
          },
          "repo_url": {
            "type": "string",
            "description": "OPTIONAL git remote URL of this repo (e.g. git@github.com:org/acme-backend.git). Machine-independent, so it rides into config.portable.json and lets /join clone the repo on a teammate's machine. MUST NOT embed credentials — /discover captures it sanitized (strips any user:token@ prefix); the validator warns if one is present. Absent ⇒ /join falls back to point-to-local for this repo. Backward compatible: existing configs without it keep working."
          },
          "type": {
            "type": "string",
            "enum": ["spring-boot", "react", "nextjs", "nestjs", "fastapi", "node-mock", "cdk", "other"],
            "description": "Tech stack type — drives agent selection"
          },
          "role": {
            "type": "string",
            "enum": ["api-service", "frontend", "mock-server", "infrastructure", "shared-lib", "other"],
            "description": "Architectural role of this repo"
          },
          "description": {
            "type": "string",
            "description": "One-line description of what this repo does"
          },
          "spec_file": {
            "type": "string",
            "description": "Path to the OpenAPI spec file relative to repo root (for api-service repos)"
          },
          "spec_copies": {
            "type": "object",
            "description": "Map of service-name → relative path where this repo keeps a local copy of that service's spec. Repo-agnostic: any consuming repo (frontend, mock-server, api-service generating a typed client, infrastructure with API Gateway integration, doc site, contract-test repo, SDK) can declare entries. /deliver Phase 4 copies the source spec to each declared path whenever Phase 3b edits the source. Keys must match service names in the top-level services map.",
            "additionalProperties": { "type": "string" }
          }
        }
      }
    },
    "services": {
      "type": "object",
      "description": "Map of service short-name → service config. Services are the logical API boundaries the pipeline operates on.",
      "additionalProperties": {
        "type": "object",
        "required": ["repo"],
        "properties": {
          "repo": {
            "type": "string",
            "description": "Key in the repos map that holds this service"
          },
          "spec_file": {
            "type": "string",
            "description": "Spec file path relative to the repo root (overrides repo-level spec_file)"
          },
          "description": {
            "type": "string"
          }
        }
      }
    },
    "domain": {
      "type": "object",
      "description": "Domain knowledge block. Optional — enriched by /init Phase B2.",
      "properties": {
        "name": { "type": "string" },
        "primary_entities": {
          "type": "array",
          "items": { "type": "string" }
        },
        "user_roles": {
          "type": "array",
          "items": { "type": "string" }
        },
        "auth_type": {
          "type": "string",
          "enum": ["jwt", "session", "api-key", "oauth2", "none"]
        },
        "i18n_languages": {
          "type": "array",
          "items": { "type": "string" }
        },
        "rtl_support": { "type": "boolean" },
        "domain_notes": { "type": "string" }
      }
    }
  }
}
